# Secrets ??never commit API keys
.env

# Debug/log artifacts from ad-hoc preprocessing runs
_debug*.txt
_preprocess_log.txt

# Raw data inputs that are only consumed by the Node build pipeline
# (03_analysis/scripts/build_app_data.js) and never fetched by the deployed site.
# They're also too large for GitHub (the business CSV/zip exceed the
# 100MB per-file hard limit). Keep them locally for reproducibility;
# re-download/re-export them if you need to rebuild from scratch.
02_data/raw/business/
02_data/raw/vworld/
02_data/raw/building_registry_manual/
02_data/raw/sgis/census_tract_stats/
02_data/raw/osm/

# OS junk
.DS_Store
Thumbs.db

# Ad-hoc local verification tooling (not part of the deployed app)
node_modules/
package.json
package-lock.json
_verify_tmp.js
